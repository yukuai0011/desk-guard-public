[project]
name = "desk-guard-public"
version = "0.1.0"
description = "GLM-4V Image Upload Test"
readme = "README.md"
requires-python = ">=3.11, <3.12"
dependencies = [
    "zhipuai>=2.1.5",
]

[tool.uv]
no-cache = true
prerelease = "allow"
preview = true
upgrade = true